# 웹 기반 규정집 관리 시스템 - 최적 솔루션 분석

## 📊 GitHub 모범 사례 조사 결과

### 발견한 핵심 시스템

#### 1. **정적 사이트 생성기 (Documentation Sites)**

| 도구 | 기술 스택 | 장점 | 단점 | 적합도 |
|------|----------|------|------|--------|
| **Docusaurus** | React (Meta) | ✅ 버전 관리 내장<br>✅ 다국어 지원(i18n)<br>✅ 검색 내장<br>✅ MDX 지원<br>✅ 블로그 기능 | ❌ React 지식 필요<br>❌ 빌드 시간 | ⭐⭐⭐⭐⭐ |
| **MkDocs** | Python | ✅ 매우 간단<br>✅ 빠른 설정<br>✅ 테마 풍부 | ❌ 고급 기능 제한<br>❌ 플러그인 필요 | ⭐⭐⭐⭐ |
| **VuePress** | Vue.js | ✅ Vue 통합<br>✅ SPA 성능<br>✅ 커스터마이징 | ❌ Vue 지식 필요<br>❌ 생태계 작음 | ⭐⭐⭐ |
| **Docsify** | JavaScript | ✅ 빌드 불필요!<br>✅ 즉시 배포<br>✅ 매우 간단 | ❌ SEO 약함<br>❌ 기능 제한 | ⭐⭐⭐⭐ |
| **Jekyll** | Ruby | ✅ GitHub Pages 기본<br>✅ 안정적 | ❌ 느림<br>❌ 구식 | ⭐⭐ |

#### 2. **문서 관리 시스템 (DMS)**

- **OpenKM**: 엔터프라이즈급 DMS, 워크플로우 자동화
- **Teedy**: 경량 DMS, 파일 버전 관리, 권한 시스템
- **Git 기반 관리**: 법률 문서 등에서 Git 활용 사례

---

## 🎯 용인대학교 규정집에 가장 적합한 솔루션

### 추천 1위: **Docusaurus + 웹 업로드 인터페이스** ⭐⭐⭐⭐⭐

**시스템 구성:**
```
[규정 담당자]
    ↓ (HWP/DOCX 업로드)
[웹 업로드 페이지]
    ↓ (자동 변환)
[GitHub Repository]
    ↓ (GitHub Actions)
[Docusaurus 빌드]
    ↓ (자동 배포)
[공개 웹사이트]
    https://yongin-regulations.github.io/
```

**장점:**
- ✅ **버전 관리 자동**: Docusaurus는 문서 버전을 자동으로 관리
- ✅ **검색 기능**: 내장 검색으로 규정 빠른 검색
- ✅ **깔끔한 UI**: 현대적이고 전문적인 디자인
- ✅ **모바일 지원**: 반응형 디자인
- ✅ **개정 이력**: Git + Docusaurus 버전 관리
- ✅ **PDF 다운로드**: 플러그인으로 PDF 생성 가능
- ✅ **접근성**: 누구나 웹에서 규정 조회 가능

**실제 사례:**
- Meta React 문서: https://react.dev/
- Facebook Open Source: https://opensource.fb.com/
- Supabase 문서: https://supabase.com/docs

---

### 추천 2위: **MkDocs + GitHub Pages** ⭐⭐⭐⭐

**시스템 구성:**
```
[웹 업로드] → [GitHub] → [MkDocs 빌드] → [GitHub Pages]
```

**장점:**
- ✅ **극도로 간단**: 설정 5분
- ✅ **빠른 빌드**: Python 기반, 매우 빠름
- ✅ **Material 테마**: 아름다운 기본 테마
- ✅ **검색 내장**: 즉시 사용 가능
- ✅ **무료**: GitHub Pages 무료 호스팅

**실제 사례:**
- Python 프로젝트들이 주로 사용
- FastAPI 문서: https://fastapi.tiangolo.com/

---

### 추천 3위: **Docsify (가장 간단)** ⭐⭐⭐⭐

**시스템 구성:**
```
[웹 업로드] → [GitHub] → [즉시 배포] (빌드 없음!)
```

**장점:**
- ✅ **빌드 불필요**: Markdown 올리면 바로 웹사이트
- ✅ **초고속 배포**: 변경 즉시 반영
- ✅ **설정 최소**: index.html 하나면 됨

**단점:**
- ❌ SEO 약함 (규정집은 크게 상관없음)
- ❌ 고급 기능 제한

---

## 💡 최종 추천: 하이브리드 시스템

### 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    규정 담당 부서                              │
└──────────────────┬──────────────────────────────────────────┘
                   │ HWP/DOCX 파일
                   ↓
┌─────────────────────────────────────────────────────────────┐
│              웹 업로드 인터페이스 (Vercel/Netlify)             │
│  - 파일 드래그 앤 드롭                                          │
│  - 규정 정보 입력 (코드, 제목, 카테고리)                          │
│  - 미리보기                                                    │
└──────────────────┬──────────────────────────────────────────┘
                   │ GitHub API로 업로드
                   ↓
┌─────────────────────────────────────────────────────────────┐
│                  GitHub Repository                           │
│  - regulations/                                              │
│  - regulations.json                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │ Push 트리거
                   ↓
┌─────────────────────────────────────────────────────────────┐
│               GitHub Actions                                 │
│  1. DOCX → MD 변환 (Pandoc)                                  │
│  2. 스마트 매칭                                                │
│  3. regulations.json 업데이트                                 │
│  4. Docusaurus 빌드                                           │
└──────────────────┬──────────────────────────────────────────┘
                   │ 자동 배포
                   ↓
┌─────────────────────────────────────────────────────────────┐
│          공개 웹사이트 (GitHub Pages/Vercel)                   │
│  https://regulations.yongin.ac.kr/                           │
│                                                              │
│  [홈] [규정 검색] [카테고리별 보기] [최근 개정]                    │
│                                                              │
│  제1편 학교법인                                                │
│    └─ 1-0-1 학교법인 단호학원 정관                              │
│  제2편 학칙                                                   │
│  제3편 학사행정                                                │
│    └─ 제1장 일반행정                                           │
│        ├─ 3-1-1 직제규정                                      │
│        └─ 3-1-2 사무분장규정                                   │
│                                                              │
│  [검색창: "교직원포상" 입력]                                     │
│  → 3-1-9 교직원포상규정 (2025-01-24 개정)                       │
│                                                              │
│  [PDF 다운로드] [변경 이력 보기] [인쇄]                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 구현 계획

### Phase 1: Docusaurus 기본 설정 (1-2시간)

```bash
# 1. Docusaurus 설치
npx create-docusaurus@latest regulations-site classic

# 2. 디렉토리 구조
regulations-site/
├── docs/                      # regulations/ 폴더와 연동
│   ├── 1-학교법인/
│   ├── 2-학칙/
│   └── 3-학사행정/
├── docusaurus.config.js       # 설정
├── sidebars.js                # 사이드바 (자동 생성)
└── src/
    └── pages/
        └── index.js           # 홈페이지
```

**설정 예시:**
```javascript
// docusaurus.config.js
module.exports = {
  title: '용인대학교 규정집',
  tagline: '용인대학교 제규정 통합 관리 시스템',
  url: 'https://yongin-regulations.github.io',
  baseUrl: '/',

  themeConfig: {
    navbar: {
      title: '용인대학교 규정집',
      items: [
        {to: '/docs/intro', label: '규정 보기', position: 'left'},
        {to: '/versions', label: '버전', position: 'left'},
      ],
    },

    // 검색 기능
    algolia: {
      appId: 'YOUR_APP_ID',
      apiKey: 'YOUR_API_KEY',
      indexName: 'yongin-regulations',
    },
  },

  // 다국어 (선택)
  i18n: {
    defaultLocale: 'ko',
    locales: ['ko', 'en'],
  },

  // 버전 관리
  docs: {
    versions: {
      current: {
        label: '최신',
      },
    },
  },
};
```

---

### Phase 2: 웹 업로드 인터페이스 (2-3시간)

**옵션 A: 간단한 HTML 페이지 (Vercel/Netlify)**

```html
<!-- upload.html -->
<!DOCTYPE html>
<html>
<head>
    <title>규정 업로드 - 용인대학교</title>
</head>
<body>
    <h1>규정 업로드</h1>

    <form id="uploadForm">
        <div>
            <label>규정 파일 (DOCX):</label>
            <input type="file" id="fileInput" accept=".docx" required>
        </div>

        <div>
            <label>규정 코드 (선택):</label>
            <input type="text" id="codeInput" placeholder="예: 3-1-9">
        </div>

        <div>
            <label>규정 제목 (선택):</label>
            <input type="text" id="titleInput" placeholder="제목이 파일에 없으면 입력">
        </div>

        <div>
            <label>카테고리:</label>
            <select id="categoryInput">
                <option value="1-학교법인">제1편 학교법인</option>
                <option value="3-학사행정/1-일반행정">제3편 학사행정 > 제1장 일반행정</option>
                <!-- ... -->
            </select>
        </div>

        <div>
            <label>개정 사유:</label>
            <textarea id="reasonInput" placeholder="예: 2025-01-24 개정"></textarea>
        </div>

        <button type="submit">업로드</button>
    </form>

    <div id="status"></div>

    <script>
        document.getElementById('uploadForm').addEventListener('submit', async (e) => {
            e.preventDefault();

            const file = document.getElementById('fileInput').files[0];
            const code = document.getElementById('codeInput').value;
            const title = document.getElementById('titleInput').value;
            const category = document.getElementById('categoryInput').value;
            const reason = document.getElementById('reasonInput').value;

            // GitHub API로 업로드
            const formData = new FormData();
            formData.append('file', file);
            formData.append('code', code);
            formData.append('title', title);
            formData.append('category', category);
            formData.append('reason', reason);

            try {
                const response = await fetch('/api/upload', {
                    method: 'POST',
                    body: formData
                });

                const result = await response.json();

                if (result.success) {
                    document.getElementById('status').innerHTML =
                        `✅ 업로드 성공! <a href="${result.url}">변경사항 보기</a>`;
                } else {
                    document.getElementById('status').textContent =
                        `❌ 업로드 실패: ${result.error}`;
                }
            } catch (error) {
                document.getElementById('status').textContent =
                    `❌ 오류: ${error.message}`;
            }
        });
    </script>
</body>
</html>
```

**옵션 B: React 앱 (더 강력)**

```bash
# React 앱 생성
npx create-react-app regulation-uploader

# 필요한 라이브러리
npm install axios react-dropzone
```

---

### Phase 3: GitHub Actions 업데이트

```yaml
# .github/workflows/docusaurus-deploy.yml
name: 규정집 자동 빌드 및 배포

on:
  push:
    paths:
      - 'regulations/**'
      - 'regulations.json'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: regulations/ → docs/ 동기화
        run: |
          # regulations 폴더를 Docusaurus docs로 복사
          rm -rf regulations-site/docs
          cp -r regulations regulations-site/docs

      - name: 사이드바 자동 생성
        run: |
          cd regulations-site
          npm run generate-sidebar

      - name: Docusaurus 빌드
        run: |
          cd regulations-site
          npm install
          npm run build

      - name: GitHub Pages 배포
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./regulations-site/build
```

---

## 📋 비교: 현재 시스템 vs 웹 기반 시스템

| 항목 | 현재 시스템 | 웹 기반 시스템 (Docusaurus) |
|------|------------|---------------------------|
| **업로드 방법** | Git CLI 또는 GitHub 웹 | 전용 웹 인터페이스 (드래그 앤 드롭) |
| **접근성** | GitHub 계정 필요 | 누구나 웹사이트 접근 가능 |
| **검색** | GitHub 검색 또는 grep | 실시간 검색, 자동완성 |
| **UI/UX** | Markdown 원본 | 전문적인 문서 사이트 |
| **버전 관리** | Git 커밋 로그 | Docusaurus 버전 + Git |
| **모바일** | 불편 | 반응형, 모바일 최적화 |
| **PDF 다운로드** | 수동 변환 | 자동 생성 버튼 |
| **변경 이력** | Git blame | UI에서 바로 확인 |
| **사용자 친화성** | ⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## 💰 비용 분석

| 구성 요소 | 무료 옵션 | 유료 옵션 (선택) |
|----------|----------|----------------|
| **호스팅** | GitHub Pages (무료) | Vercel Pro ($20/월) |
| **검색** | Docusaurus 기본 검색 | Algolia DocSearch (무료) |
| **업로드 서버** | Vercel Functions (무료) | AWS Lambda (사용량 기반) |
| **도메인** | github.io | regulations.yongin.ac.kr ($10/년) |
| **총 비용** | **$0/월** | **$20-30/월** (선택) |

**추천**: 무료 옵션으로 시작, 필요시 유료 전환

---

## ⏱️ 구현 일정

### 최소 버전 (1-2일)
- Day 1: Docusaurus 설정, 기존 규정 마이그레이션
- Day 2: GitHub Actions 설정, 테스트 배포

### 완전 버전 (3-5일)
- Day 1: Docusaurus 설정
- Day 2: 웹 업로드 인터페이스 개발
- Day 3: GitHub API 연동
- Day 4: 테스트 및 버그 수정
- Day 5: 문서화 및 배포

---

## 🎯 다음 단계

어떤 방향으로 진행하시겠습니까?

1. **최소 버전**: Docusaurus만 먼저 구축 (빠름)
2. **완전 버전**: 웹 업로드 + Docusaurus (추천)
3. **초간단**: Docsify로 빠르게 시작

선택해주시면 바로 구현 시작하겠습니다!
